import "../styles/header.css";

const Header = () => {
  return (
    <header className="header">
      <h1>Employee Directory</h1>
      <p>
        Use the search box to narrow your results or click the sort button to
        sort by name.
      </p>
    </header>
  );
};

export default Header;
